<!-- Type narrowing does not mean concise or smaller code. -->
In TypeScript, type narrowing means reducing a variable’s possible types into a more specific type using checks like typeof, instanceof, or conditions.It helps TypeScript understand the exact type of a value in a specific block of code.

<!-- Example -->
function print(value: string | number) {
  if (typeof value === "string") {
    console.log(value.toUpperCase());
  } else {
    console.log(value.toFixed(2));
  }
}

Here,value can be string or number.Inside the if block, TypeScript narrows the type to string.Inside the else block, it narrows the type to number.So,This process is called type narrowing.

Type narrowing improves:

type safety
better error checking
smarter autocomplete
safer code execution

Sometimes it can also make code cleaner, but its main purpose is making types more specific and safe.